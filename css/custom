/* ==========================================================================
   Стол-Профи — кастомные стили
   ========================================================================== */

:root {
    --grid-line-color: rgba(15, 23, 42, 0.08);
    --grid-line-color-dark: rgba(248, 250, 252, 0.06);
    --accent-glow: rgba(234, 88, 12, 0.55);
    --scrollbar-track: #0F172A;
    --scrollbar-thumb: #334155;
    --scrollbar-thumb-hover: #EA580C;
}

/* ==========================================================================
   1. Координатная сетка (имитация столешницы с отверстиями)
   ========================================================================== */

.tabletop-grid {
    background-color: #F8FAFC;
    background-image:
        linear-gradient(var(--grid-line-color) 1px, transparent 1px),
        linear-gradient(90deg, var(--grid-line-color) 1px, transparent 1px);
    background-size: 50px 50px;
    background-position: center;
}

.tabletop-grid-lg {
    background-color: #F8FAFC;
    background-image:
        linear-gradient(var(--grid-line-color) 1px, transparent 1px),
        linear-gradient(90deg, var(--grid-line-color) 1px, transparent 1px);
    background-size: 100px 100px;
    background-position: center;
}

.tabletop-grid-dark {
    background-color: #0F172A;
    background-image:
        linear-gradient(var(--grid-line-color-dark) 1px, transparent 1px),
        linear-gradient(90deg, var(--grid-line-color-dark) 1px, transparent 1px);
    background-size: 50px 50px;
    background-position: center;
}

/* Имитация перфорации: отверстия в узлах сетки шагом 50 мм */
.tabletop-holes {
    position: relative;
    background-color: #F8FAFC;
    background-image:
        radial-gradient(circle, rgba(15, 23, 42, 0.18) 1.5px, transparent 1.5px),
        linear-gradient(var(--grid-line-color) 1px, transparent 1px),
        linear-gradient(90deg, var(--grid-line-color) 1px, transparent 1px);
    background-size: 50px 50px, 50px 50px, 50px 50px;
    background-position: center;
}

.tabletop-holes-dark {
    position: relative;
    background-color: #0F172A;
    background-image:
        radial-gradient(circle, rgba(248, 250, 252, 0.22) 1.5px, transparent 1.5px),
        linear-gradient(var(--grid-line-color-dark) 1px, transparent 1px),
        linear-gradient(90deg, var(--grid-line-color-dark) 1px, transparent 1px);
    background-size: 50px 50px, 50px 50px, 50px 50px;
    background-position: center;
}

/* ==========================================================================
   2. Технические бейджи характеристик
   ========================================================================== */

.badge-tech {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.25rem 0.75rem;
    border-radius: 0.25rem;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    line-height: 1.4;
    white-space: nowrap;
    border: 1px solid transparent;
}

.badge-tech i,
.badge-tech svg {
    font-size: 0.7rem;
}

/* Базовый вариант — нейтральная сталь */
.badge-tech-default {
    background-color: #E2E8F0;
    color: #1E293B;
    border-color: #CBD5E1;
}

/* Система быстрозажимной оснастки (D16 / D28) */
.badge-tech-system {
    background-color: rgba(15, 23, 42, 0.04);
    color: #0F172A;
    border-color: #1E293B;
}

/* Термообработка / азотирование */
.badge-tech-treatment {
    background-color: rgba(234, 88, 12, 0.08);
    color: #C2410C;
    border-color: rgba(234, 88, 12, 0.35);
}

/* Марка стали / материал */
.badge-tech-material {
    background-color: rgba(51, 65, 85, 0.06);
    color: #334155;
    border-color: #94A3B8;
}

/* Тёмный вариант бейджа для тёмных секций */
.badge-tech-dark {
    background-color: rgba(248, 250, 252, 0.08);
    color: #F8FAFC;
    border-color: rgba(248, 250, 252, 0.25);
}

/* ==========================================================================
   3. Промышленный эффект свечения (glow)
   ========================================================================== */

.btn-glow {
    position: relative;
    transition: box-shadow 0.3s ease, transform 0.2s ease, background-color 0.3s ease;
    box-shadow: 0 0 0 rgba(234, 88, 12, 0);
}

.btn-glow:hover,
.btn-glow:focus-visible {
    box-shadow: 0 0 18px 4px var(--accent-glow), 0 4px 14px rgba(15, 23, 42, 0.25);
    transform: translateY(-1px);
}

.btn-glow:active {
    transform: translateY(0);
    box-shadow: 0 0 10px 2px var(--accent-glow);
}

/* Пульсирующее свечение для акцентных CTA-элементов */
@keyframes glow-pulse {
    0%, 100% {
        box-shadow: 0 0 8px 1px rgba(234, 88, 12, 0.35);
    }
    50% {
        box-shadow: 0 0 20px 6px rgba(234, 88, 12, 0.6);
    }
}

.btn-glow-pulse {
    animation: glow-pulse 2.4s ease-in-out infinite;
}

/* Hover-эффект для карточек оборудования */
.card-equipment {
    position: relative;
    transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
    border: 1px solid #E2E8F0;
    overflow: hidden;
}

.card-equipment::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(234, 88, 12, 0.08), transparent 60%);
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
}

.card-equipment:hover {
    transform: translateY(-4px);
    border-color: rgba(234, 88, 12, 0.5);
    box-shadow: 0 10px 30px rgba(15, 23, 42, 0.12), 0 0 16px rgba(234, 88, 12, 0.15);
}

.card-equipment:hover::before {
    opacity: 1;
}

/* Свечение по контуру иконки/значка при наведении на карточку */
.card-equipment:hover .card-equipment-icon {
    color: #EA580C;
    filter: drop-shadow(0 0 6px rgba(234, 88, 12, 0.6));
}

/* ==========================================================================
   4. Плавная прокрутка и кастомный скроллбар
   ========================================================================== */

html {
    scroll-behavior: smooth;
}

/* Webkit-браузеры (Chrome, Edge, Safari, Opera) */
::-webkit-scrollbar {
    width: 10px;
    height: 10px;
}

::-webkit-scrollbar-track {
    background: var(--scrollbar-track);
}

::-webkit-scrollbar-thumb {
    background-color: var(--scrollbar-thumb);
    border-radius: 6px;
    border: 2px solid var(--scrollbar-track);
}

::-webkit-scrollbar-thumb:hover {
    background-color: var(--scrollbar-thumb-hover);
}

::-webkit-scrollbar-corner {
    background: var(--scrollbar-track);
}

/* Firefox */
* {
    scrollbar-width: thin;
    scrollbar-color: var(--scrollbar-thumb) var(--scrollbar-track);
}

/* Учёт фиксированного хедера при переходе по якорям */
[id] {
    scroll-margin-top: 96px;
}
